Counting objects: 3, done.
Delta compression using up to 8 threads.
Compressing objects: 100% (2/2), done.
Writing objects: 100% (3/3), 343 bytes | 114.00 KiB/s, done.
Total 3 (delta 1), reused 0 (delta 0)
remote: Resolving deltas: 100% (1/1), completed with 1 local object.
To https://github.com/hhd14/Assignment1.git
   fbdbe93..48d9a2b  master -> master

There have been 9 commits so far
I used the command git log

October 8th at 11:38
I used the command ls -l .gitignore

 1cc7d6674cce7e5339598e87e07e329c45814408 (origin/development)
 a46d9263dfca5ecc9a803da298fcb3b2e229131f

To develop features isolated from each other.
They tend to be easier than cloned directories for pulling in changes and for sharing changes across multiple projects.

git log gets the information about the last commits, while get status shows the working tree status.

The command is git log --follow Weapon.h

The command is git log --grep="file"

It enables new classes to receive or "inherit" the properties and methods of existing classes.
It's a programming language's ability to process objects differently depending on their data type or class.
A concept that binds together the data and functions that manipulate the data, and that keeps both safe from outside interference and misuse.

The RoleGame prototype is using polymorphism. By having our base classes SimpleAxe, CrazyRandomSword, and Wand derived from the class Weapon, we
have created a hierarchy of classes that are related by inheritance.
